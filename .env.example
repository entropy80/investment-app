# =============================================================================
# Investment App - Environment Variables
# =============================================================================
# Copy this file to .env.local for development or .env.production.local for production
# Never commit actual secrets to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
# The public URL of your application (used for redirects, emails, etc.)
# Use localhost for development, your domain for production
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Database (Neon Serverless PostgreSQL)
# -----------------------------------------------------------------------------
# Get connection strings from Neon dashboard: https://console.neon.tech

# Pooled connection (for app queries at runtime)
# Format: postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Direct connection (for Prisma migrations - no connection pooler)
# Format: postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# -----------------------------------------------------------------------------
# Authentication (NextAuth.js)
# -----------------------------------------------------------------------------
# The canonical URL of your site (must match NEXT_PUBLIC_APP_URL in production)
# Use localhost for development, your domain for production
NEXTAUTH_URL=http://localhost:3000

# Secret for encrypting sessions and signing JWTs (minimum 32 characters)
# Generate with: openssl rand -base64 48
NEXTAUTH_SECRET=

# -----------------------------------------------------------------------------
# Email (Resend)
# -----------------------------------------------------------------------------
# Get API key from https://resend.com/api-keys
RESEND_API_KEY=re_...

# Sender email address (must be verified domain in Resend, or use test address)
EMAIL_FROM="Investment App <noreply@example.com>"

# -----------------------------------------------------------------------------
# Price APIs
# -----------------------------------------------------------------------------
# Financial Modeling Prep API (primary price source)
# Get API key from https://financialmodelingprep.com/developer/docs/
FMP_API_KEY=

# Alpha Vantage API (fallback price source)
# Get API key from https://www.alphavantage.co/support/
ALPHA_VANTAGE_API_KEY=

# -----------------------------------------------------------------------------
# Storage (for document uploads)
# -----------------------------------------------------------------------------
# Storage provider: vercel_blob, local, or s3
# If not set, auto-detects: vercel_blob on Vercel, local otherwise
# STORAGE_PROVIDER=local

# Vercel Blob (used when STORAGE_PROVIDER=vercel_blob)
# Automatically set by Vercel when Blob Storage is enabled
# BLOB_READ_WRITE_TOKEN=

# Local Storage (used when STORAGE_PROVIDER=local)
# Directory for file uploads (relative to project root or absolute path)
STORAGE_LOCAL_PATH=./uploads
# URL path prefix for serving local files
STORAGE_LOCAL_URL=/api/storage

# S3-Compatible Storage (used when STORAGE_PROVIDER=s3) - Not yet implemented
# S3_BUCKET=
# S3_REGION=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_ENDPOINT=                # For MinIO or S3-compatible services

# -----------------------------------------------------------------------------
# Cron Jobs (Vercel)
# -----------------------------------------------------------------------------
# Secret token to authenticate cron job requests
# Generate with: openssl rand -base64 32
CRON_SECRET=
